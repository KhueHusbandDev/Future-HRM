import 'package:flutter/material.dart';

class FunctionCard extends StatelessWidget {
  String title;

  FunctionCard({super.key, required this.title});

  @override
  Widget build(BuildContext context) {
    return Card();
  }
}
